// Injected at build time by vite.config.ts (`define`) — the app's build stamp
// and the released version (vX.Y.Z from the git tag; "0.0.0+dev" locally).
declare const __BUILD_ID__: string;
declare const __APP_VERSION__: string;
